name: Portfolio heartbeat

on:
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:

permissions:
  contents: read

env:
  API_URL: https://atlas-sac-web.vercel.app
  WEB_URL: https://atlas-sac-ui.vercel.app

jobs:
  heartbeat:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Verify API and Supabase readiness
        shell: bash
        run: |
          set -euo pipefail
          for attempt in 1 2 3; do
            curl \
              --fail \
              --show-error \
              --silent \
              --retry 2 \
              --retry-all-errors \
              --connect-timeout 10 \
              --max-time 30 \
              "$API_URL/health"
            echo
            if [ "$attempt" -lt 3 ]; then sleep 3; fi
          done

      - name: Warm deterministic ML path
        run: |
          curl \
            --fail \
            --show-error \
            --silent \
            --retry 2 \
            --retry-all-errors \
            --connect-timeout 10 \
            --max-time 45 \
            "$API_URL/v1/ml/evaluation" \
            > /dev/null

      - name: Verify public web entry points
        run: |
          curl \
            --fail \
            --show-error \
            --silent \
            --retry 2 \
            --retry-all-errors \
            --connect-timeout 10 \
            --max-time 30 \
            "$WEB_URL" \
            > /dev/null
          curl \
            --fail \
            --show-error \
            --silent \
            --retry 2 \
            --retry-all-errors \
            --connect-timeout 10 \
            --max-time 30 \
            "$WEB_URL/micro" \
            > /dev/null
